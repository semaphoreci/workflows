# For more Clojure information and examples, see:
# https://docs.semaphoreci.com/article/112-language-clojure
version: v1.0
name: Hello Semaphore
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Clojure example
    task:
      jobs:
      - name: Build a project
        commands:
          - lein new app hello && cd hello
          - lein run
          # Uncomment the following line to pull your code,
          # then proceed by adding your custom commands:
          #- checkout
